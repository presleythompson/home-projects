# Local development: point at a local Postgres (uses the pg driver)
# DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/home_tasks

# Production / Neon: paste the Neon connection string here (uses the serverless driver)
# DATABASE_URL=postgresql://<user>:<password>@<host>.neon.tech/<db>?sslmode=require
DATABASE_URL=
